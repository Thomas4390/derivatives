# Dependencies for the Calibration Streamlit app (app + common + backend).
# numpy is left unpinned so numba can resolve a compatible version.
streamlit>=1.38.0
streamlit-code-editor>=0.1.22
plotly>=6.0.0
pandas>=2.3.1
pyarrow
scipy>=1.16.0
numba>=0.61.2
jax>=0.10.0
jaxlib>=0.10.0
