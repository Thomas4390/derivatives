"""Dark-theme constants for the simulation app — re-exported from `streamlit_app.common`.

Kept here for backward compatibility with simulation-internal imports.
The single source of truth lives in `streamlit_app/common/chart_theme.py`.
"""

from streamlit_app.common.chart_theme import (  # noqa: F401
    ANN_BG,
    ANN_FONT,
    AXIS_LABEL,
    AXIS_LINE,
    AXIS_STYLE,
    CHART_HEIGHT_LG,
    CHART_HEIGHT_MD,
    CHART_HEIGHT_SM,
    GRID,
    LEGEND_COLOR,
    PAPER_BG,
    PLOT_BG,
    TICK_COLOR,
    badge,
)
