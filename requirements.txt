# Runtime dependencies for the Streamlit apps (options_greeks, simulation, calibration)
# Streamlit Community Cloud installs ONLY what is listed here.
numpy>=2.0,<2.4
scipy>=1.15.0
numba>=0.62.0
streamlit>=1.40.0
plotly>=5.0.0

# simulation + calibration (dataframes / parquet snapshots)
pandas>=2.2.3
pyarrow>=15.0.0

# calibration solvers (LM-JAX) + backend.calibration objectives
jax>=0.10.0
jaxlib>=0.10.0

# simulation custom-model editor (provides the `code_editor` module)
streamlit-code-editor>=0.1.22
